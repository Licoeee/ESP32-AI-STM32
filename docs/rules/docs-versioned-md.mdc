---
description: Enforce versioned documentation folders under docs
alwaysApply: true
---

# 文档按版本号分目录

当需要在 `docs/` 下生成或新增 `.md` 文档时，必须按照版本号进行目录划分，版本号格式统一为 `V1.0.0`。

## 执行要求

1. 每个版本单独创建一个文件夹，例如 `docs/V1.0.0/`。
2. 该版本产生的所有 Markdown 文档，都必须放入对应版本文件夹中。
3. 新建文档前，先确认当前文档对应的版本号，再决定存放路径。
4. 不得将版本文档直接散落在 `docs/` 根目录下。
5. 如果版本号不明确，先向用户确认版本号后再创建文档。

## 适用条件

- 适用于每次生成 `.md` 文档时
- 适用于 `docs/` 目录及其子目录中的版本化文档
- 规则目标是保持文档可追溯、易归档、按版本管理
