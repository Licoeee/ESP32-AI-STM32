---
description: Force pin-related tasks to read hardware pin definition docs first
alwaysApply: true
---

# 引脚配置优先读取规则

当当前任务涉及以下内容时，必须先读取 `docs/引脚定义.md`，并以其作为本项目引脚配置的优先参考：

- GPIO / 引脚选择
- UART / SPI / I2C / PWM / ADC / DAC
- USB1 / USB2 / OTG / 串口烧录 / 调试口
- 板载 LED / RGB 灯 / 按键 / 复位脚 / 启动脚
- 任何新增外设接线或硬件映射

## 执行要求

1. 在给出引脚方案、修改代码、生成配置前，先阅读 `docs/引脚定义.md`。
2. 不得擅自使用未在文档中确认的 GPIO 作为默认方案。
3. 如果代码、注释或临时假设与文档冲突，优先以文档为准，并明确指出差异。
4. 如果文档未覆盖某个引脚或外设，必须先向用户确认硬件连接，再继续。
5. 修改硬件连接、引脚映射或板卡版本时，应同步更新 `docs/引脚定义.md`。

## 适用原则

这条规则的目标不是限制开发，而是确保引脚相关决策始终基于当前板卡的真实硬件定义，避免误用 GPIO 导致烧录失败、外设冲突或硬件损坏。
